# MongoDB connection string (required on Render — use MongoDB Atlas)
# Example: mongodb+srv://user:password@cluster.mongodb.net/live-auction?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/live-auction

# Render sets PORT automatically; only needed for local overrides
# PORT=8080
